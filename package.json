{
  "name": "autentique-mock",
  "version": "1.0.0",
  "private": true,
  "description": "Mock local da API GraphQL da Autentique, para testar integracoes de geracao e assinatura de documentos sem depender da nuvem real.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "DOWNLOADS_DIR=/tmp/autentique-mock-test MAX_UPLOAD_BYTES=1024 node --test src/*.test.js src/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "graphql": "^16.14.2",
    "multer": "^2.4.0",
    "pdf-lib": "^1.17.1"
  }
}
