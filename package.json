{
  "name": "autentique-mock",
  "version": "1.0.0",
  "private": true,
  "description": "Mock local da API GraphQL da Autentique, para testar integracoes de geracao e assinatura de documentos sem depender da nuvem real.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "DOWNLOADS_DIR=/tmp/autentique-mock-test node --test src"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "pdf-lib": "^1.17.1"
  }
}
